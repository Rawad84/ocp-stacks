# Claude Code local settings
.claude/

# Deployment logs — may contain secrets extracted from cluster
*.log

# Docs folders — kept locally, not tracked in git
**/docs/

# All markdown files except README.md (applies to all stacks)
*.md
!README.md
!**/README.md

# env.sh.local can override env.sh with real credentials — never commit
**/env.sh.local

# Kubeconfig and auth files
kubeconfig
auth/
*.kubeconfig
